# Tech-Talks Recipe Review & Regeneration Workflow

Comprehensive workflow to review and regenerate all tech-talk decks. Three stages per talk: recipe review → slide regeneration → build/test.

---

## Agent Teams
- [x] **Stage 1: Recipe Review** — Run `deck-recipe-review` skill on `tech-talks/agent-teams`
- [ ] **Stage 2: Regenerate Slides** — Invoke `Slide Generator` agent with `tech-talks/agent-teams`
- [ ] **Stage 3: Build & Test** — Run `.\build.ps1 Tech-talks` and verify `agent-teams.md` builds with no errors

## Agentic Journey
- [ ] **Stage 1: Recipe Review** — Run `deck-recipe-review` skill on `tech-talks/agentic-journey`
- [ ] **Stage 2: Regenerate Slides** — Invoke `Slide Generator` agent with `tech-talks/agentic-journey`
- [ ] **Stage 3: Build & Test** — Run `.\build.ps1 Tech-talks` and verify `agentic-journey.md` builds with no errors

## Agentic SDLC
- [ ] **Stage 1: Recipe Review** — Run `deck-recipe-review` skill on `tech-talks/agentic-sdlc`
- [ ] **Stage 2: Regenerate Slides** — Invoke `Slide Generator` agent with `tech-talks/agentic-sdlc`
- [ ] **Stage 3: Build & Test** — Run `.\build.ps1 Tech-talks` and verify `agentic-sdlc.md` builds with no errors

## Agentic Workflows
- [ ] **Stage 1: Recipe Review** — Run `deck-recipe-review` skill on `tech-talks/agentic-workflows`
- [ ] **Stage 2: Regenerate Slides** — Invoke `Slide Generator` agent with `tech-talks/agentic-workflows`
- [ ] **Stage 3: Build & Test** — Run `.\build.ps1 Tech-talks` and verify `agentic-workflows.md` builds with no errors

## Copilot ACP
- [ ] **Stage 1: Recipe Review** — Run `deck-recipe-review` skill on `tech-talks/copilot-acp`
- [ ] **Stage 2: Regenerate Slides** — Invoke `Slide Generator` agent with `tech-talks/copilot-acp`
- [ ] **Stage 3: Build & Test** — Run `.\build.ps1 Tech-talks` and verify `copilot-acp.md` builds with no errors

## Copilot Azure MCP
- [ ] **Stage 1: Recipe Review** — Run `deck-recipe-review` skill on `tech-talks/copilot-azure-mcp`
- [ ] **Stage 2: Regenerate Slides** — Invoke `Slide Generator` agent with `tech-talks/copilot-azure-mcp`
- [ ] **Stage 3: Build & Test** — Run `.\build.ps1 Tech-talks` and verify `copilot-azure-mcp.md` builds with no errors

## Copilot Chat Internals
- [ ] **Stage 1: Recipe Review** — Run `deck-recipe-review` skill on `tech-talks/copilot-chat-internals`
- [ ] **Stage 2: Regenerate Slides** — Invoke `Slide Generator` agent with `tech-talks/copilot-chat-internals`
- [ ] **Stage 3: Build & Test** — Run `.\build.ps1 Tech-talks` and verify `copilot-chat-internals.md` builds with no errors

## Copilot CLI
- [ ] **Stage 1: Recipe Review** — Run `deck-recipe-review` skill on `tech-talks/copilot-cli`
- [ ] **Stage 2: Regenerate Slides** — Invoke `Slide Generator` agent with `tech-talks/copilot-cli`
- [ ] **Stage 3: Build & Test** — Run `.\build.ps1 Tech-talks` and verify `copilot-cli.md` builds with no errors

## Copilot Code Review
- [ ] **Stage 1: Recipe Review** — Run `deck-recipe-review` skill on `tech-talks/copilot-code-review`
- [ ] **Stage 2: Regenerate Slides** — Invoke `Slide Generator` agent with `tech-talks/copilot-code-review`
- [ ] **Stage 3: Build & Test** — Run `.\build.ps1 Tech-talks` and verify `copilot-code-review.md` builds with no errors

## Copilot Hooks
- [ ] **Stage 1: Recipe Review** — Run `deck-recipe-review` skill on `tech-talks/copilot-hooks`
- [ ] **Stage 2: Regenerate Slides** — Invoke `Slide Generator` agent with `tech-talks/copilot-hooks`
- [ ] **Stage 3: Build & Test** — Run `.\build.ps1 Tech-talks` and verify `copilot-hooks.md` builds with no errors

## Copilot Memory
- [ ] **Stage 1: Recipe Review** — Run `deck-recipe-review` skill on `tech-talks/copilot-memory`
- [ ] **Stage 2: Regenerate Slides** — Invoke `Slide Generator` agent with `tech-talks/copilot-memory`
- [ ] **Stage 3: Build & Test** — Run `.\build.ps1 Tech-talks` and verify `copilot-memory.md` builds with no errors

## Copilot Plugins
- [ ] **Stage 1: Recipe Review** — Run `deck-recipe-review` skill on `tech-talks/copilot-plugins`
- [ ] **Stage 2: Regenerate Slides** — Invoke `Slide Generator` agent with `tech-talks/copilot-plugins`
- [ ] **Stage 3: Build & Test** — Run `.\build.ps1 Tech-talks` and verify `copilot-plugins.md` builds with no errors

## Copilot Primitives
- [ ] **Stage 1: Recipe Review** — Run `deck-recipe-review` skill on `tech-talks/copilot-primitives`
- [ ] **Stage 2: Regenerate Slides** — Invoke `Slide Generator` agent with `tech-talks/copilot-primitives`
- [ ] **Stage 3: Build & Test** — Run `.\build.ps1 Tech-talks` and verify `copilot-primitives.md` builds with no errors

## Copilot SDK
- [ ] **Stage 1: Recipe Review** — Run `deck-recipe-review` skill on `tech-talks/copilot-sdk`
- [ ] **Stage 2: Regenerate Slides** — Invoke `Slide Generator` agent with `tech-talks/copilot-sdk`
- [ ] **Stage 3: Build & Test** — Run `.\build.ps1 Tech-talks` and verify `copilot-sdk.md` builds with no errors

## Copilot Web
- [ ] **Stage 1: Recipe Review** — Run `deck-recipe-review` skill on `tech-talks/copilot-web`
- [ ] **Stage 2: Regenerate Slides** — Invoke `Slide Generator` agent with `tech-talks/copilot-web`
- [ ] **Stage 3: Build & Test** — Run `.\build.ps1 Tech-talks` and verify `copilot-web.md` builds with no errors

## Enterprise Patterns
- [ ] **Stage 1: Recipe Review** — Run `deck-recipe-review` skill on `tech-talks/enterprise-patterns`
- [ ] **Stage 2: Regenerate Slides** — Invoke `Slide Generator` agent with `tech-talks/enterprise-patterns`
- [ ] **Stage 3: Build & Test** — Run `.\build.ps1 Tech-talks` and verify `enterprise-patterns.md` builds with no errors

## MCP Apps
- [ ] **Stage 1: Recipe Review** — Run `deck-recipe-review` skill on `tech-talks/mcp-apps`
- [ ] **Stage 2: Regenerate Slides** — Invoke `Slide Generator` agent with `tech-talks/mcp-apps`
- [ ] **Stage 3: Build & Test** — Run `.\build.ps1 Tech-talks` and verify `mcp-apps.md` builds with no errors

## VS Code Latest
- [ ] **Stage 1: Recipe Review** — Run `deck-recipe-review` skill on `tech-talks/vscode-latest`
- [ ] **Stage 2: Regenerate Slides** — Invoke `Slide Generator` agent with `tech-talks/vscode-latest`
- [ ] **Stage 3: Build & Test** — Run `.\build.ps1 Tech-talks` and verify `vscode-latest.md` builds with no errors

---

## Notes

- **Stage 1 outputs**: Updated `deck.recipe.yml` with max 4 sections, emphasis levels, narrative arc comment
- **Stage 2 outputs**: Regenerated `slides/tech-talks/<talk>.md` with consistent structure via TEMPLATE.md
- **Stage 3 validation**: Full build completes with no Vue parse errors, no UNRESOLVED_IMPORT warnings
- **Parallel execution**: Stages 1 & 2 can run in parallel across talks; Stage 3 (build) should be run once across all talks: `.\build.ps1 Tech-talks`

## Progress Tracking

**Completed:** 0 / 18
**In Progress:** —
**Blocked:** —
