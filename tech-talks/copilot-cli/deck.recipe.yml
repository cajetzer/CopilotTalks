# deck.recipe.yml — Copilot CLI slide-generation recipe

version: 1

source: tech-talks/copilot-cli/README.md

deck:
  title: GitHub Copilot CLI
  subtitle: Terminal-Native AI
  tagline: For Developers & DevOps Engineers

  intro:
    mode: core-question

  toc:
    enabled: true

  # Major sections from 🎬 markers
  sectionOrder:
    - Plan Mode & Steering
    - Operating Modes
    - Built-in Agents
    - Use Cases

  sectionModes:
    Plan Mode & Steering: deep-dive
    Operating Modes: balanced
    Built-in Agents: balanced
    Use Cases: deep-dive

  # Newest/most differentiated capabilities to feature prominently
  highlightMoments:
    - /fleet fan-out explicitly decomposes parallel subtasks
    - GitHub.com integration from terminal (issues, PRs without browser)
    - Cloud delegation with & prefix completely frees IDE and terminal

  references:
    enabled: true
    mode: standard

  thankYou:
    enabled: true
    variant: standard
